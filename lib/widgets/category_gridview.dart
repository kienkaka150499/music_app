import 'package:flutter/material.dart';

class CategoryGridView extends StatelessWidget{
  String category;

  CategoryGridView({required this.category});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container();
  }
}